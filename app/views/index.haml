%h2
  Current links:

- if @links != nil
  #links-container
    %ul#links
      - @links.each do |link|
        = partial :link, :locals => {:link => link}

%aside
%h2
  Tags

- if @tags != nil
  - @tags.each do |tag|
    - tag_name = tag.text
    - tag_url = "/tags/#{tag_name}"
    %li
      %a(href=tag_url)
        = tag_name